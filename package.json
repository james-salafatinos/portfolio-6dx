{
  "name": "portfolio-6dx",
  "version": "0.2.0",
  "private": true,
  "description": "An agent-ready, low-friction laboratory for visualizations and experiments.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "validate": "node scripts/validate-experiments.js",
    "test": "node --test tests/*.test.js && npm run validate",
    "smoke": "node scripts/smoke-browser.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.1.0",
    "lil-gui": "^0.20.0",
    "marked": "^15.0.12",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
